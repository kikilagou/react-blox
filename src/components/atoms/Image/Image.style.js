import styled from "styled-components";
import theme from "../../../styles/theme";

const ImageWrapper = styled.img`
    height: ${theme.values.imageHeight}
    width: ${theme.values.imageWidth}
`;

export { ImageWrapper };
